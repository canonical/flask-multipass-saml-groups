name: Tests

on:
  pull_request:

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@pre-run-script
    secrets: inherit
    with:
      pre-run-script: install-packages.sh